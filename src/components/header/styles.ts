import { styled } from "@stitches/react";

export const HeaderContainer = styled('header', {
    padding: '2rem 0',
    width: '100%',
    maxWidth: 1180,
    margin: 'auto',
    
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',

    button: {
        marginLeft: 'auto'
    }

})